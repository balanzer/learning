
/*
interface Todo {
  name: String;
  completed: boolean;
  dateCompleted?: Date;
}
interface TodoService {
  getTodo(id: number): Todo;
  delegetTodo(id: number): void;
}

interface jQueryElement {}

interface jQuery {
  version: number;
  fn: any;
  (selector: string | any): jQueryElement;
}

let todo: Todo = { name: "hello", completed: false };
*/